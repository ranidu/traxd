module.exports = {
  semi: false,
  singleQuote: true,
  trailingComma: "all",
  printWidth: 100,
  tabWidth: 2,
  bracketSpacing: true,
  arrowParens: "avoid", // const double = x => x * 2 instead of const double = (x) => x * 2, but still double = (x, y) => x + y
};
